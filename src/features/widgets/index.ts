export async function getWidgetConfig(tenantId: string) {
  // Placeholder for widget config logic
  return {
    tenantId,
    config: {},
  };
}

export async function generateEmbedCode(tenantId: string) {
  // Placeholder for embed code generator
  return `<script src="https://your-domain.com/widget/embed?tenant=${tenantId}"></script>`;
}
